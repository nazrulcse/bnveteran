source 'https://rubygems.org'
#ruby '~> 2.4.0'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '5.0.2'
gem 'bootstrap-datepicker-rails', :require => 'bootstrap-datepicker-rails'
gem 'pg'
gem 'wicked_pdf'
gem 'wkhtmltopdf-binary'
gem 'delayed_job_active_record'
gem 'daemons'

# Use SCSS for stylesheets
#gem 'sass-rails', '~> 5.0.0'
# Use Uglifier as compressor for JavaScript assets
group :assets do
 # gem 'therubyracer'
  gem 'therubyracer',  platforms: :ruby
 # gem 'sass-rails', "  ~> 3.1.0"
  gem 'sass-rails', '~> 5.0.0'
  gem 'coffee-rails', "~> 4.2.2"
  gem 'uglifier', '>= 1.0.3', require: 'uglifier'
end

# Use jquery as the JavaScript library
gem 'jquery-rails'
gem 'merit'
# Used to implement at.js for auto complete mentions/emojis
gem 'jquery-atwho-rails'

# Use twitter bootstrap sass
#gem 'bootstrap-sass', '~> 3.2.0'
gem 'bootstrap-sass', '~> 3.3.6'
gem 'active_bootstrap_skin'
gem 'autoprefixer-rails'
gem 'font-awesome-rails'
gem 'figaro'

group :development do
  gem 'spring'
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'letter_opener'
  gem 'guard'
  gem 'guard-rspec', '~> 4.2.8'
  gem 'annotate'
end

group :development, :test do
  gem 'puma'
  gem 'pry'
end

group :production do

  gem 'unicorn'
  # gem 'rails_12factor'
  gem 'fog'
  gem 'fog-aws'
end

gem 'devise'
gem 'carrierwave'
gem 'friendly_id', '~> 5.0'

gem 'will_paginate', '~> 3.1.0'
gem 'public_activity'

gem 'acts_as_votable', '~> 0.10.0'
gem 'acts_as_commentable'
gem 'acts_as_follower'
gem 'counter_culture', '~> 0.1.33'

gem 'faker'
gem 'populator'
gem 'auto_html', '~>1.6.4'
gem 'sanitize'

gem 'active_model_serializers'

gem "rolify"
gem 'activeadmin'

gem 'country_select'

group :development do
 gem "capistrano", '~>3.8.1'
 gem 'net-ssh'
 gem 'capistrano-bundler'
 gem 'capistrano-rails'
 gem 'capistrano-rvm'
end

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
#gem 'therubyracer',  platforms: :ruby

# for error notifier from application
gem 'exception_notification'

# hashtag counting..
gem 'simple_hashtag'
# at this moment the latest stable version
gem 'rmagick', '2.16.0'
# integrates the TinyMCE editor with the Rails asset pipeline
gem 'tinymce-rails'
# DropzoneJS is an open source library that provides drag’n’drop file uploads with image previews
gem 'dropzonejs-rails'
# for seo friendly sitemap_generator added
gem 'sitemap_generator'
# for authorization
gem 'cancancan', '~> 2.0'

